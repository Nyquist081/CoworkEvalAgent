[project]
name = "coworkeval-agent"
version = "0.1.0"
description = "CoworkEval Agent Sidecar"
requires-python = ">=3.12"
dependencies = [
    "httpx>=0.27.0",
    "pydantic>=2.0",
]

[dependency-groups]
dev = [
    "pytest>=9.0.3",
]
